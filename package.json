{
  "name": "brodul-inztagram",
  "dependencies": {
    "autoprefixer": "^10.1.0",
    "postcss": "^8.2.1",
    "tailwindcss": "^2.0.2"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1",
    "postcss-modules": "^4.0.0"
  },
  "scripts": {
    "build": "rm -rf .cache dist && parcel build src/index.html --public-url ./",
    "serve": "npx parcel src/index.html"
  },
  "license": "MIT"
}
